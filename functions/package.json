{
  "name": "trackmark-money-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "node --test",
    "test:rules": "cd .. && firebase emulators:exec --config firebase.emulator.json --only firestore --project demo-trackmark-deletion 'node functions/integration/account_deletion_rules.js'"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "eslint": "^9.30.1",
    "globals": "^16.3.0"
  }
}
